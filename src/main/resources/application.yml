spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fbshomework?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.fbshomework.entity

server:
  port: 8080